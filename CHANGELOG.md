# 更新日志

本项目的全部重要变更都会记录在此文件中。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。


## [1.2.0] - 2026-09-26

### 变更

- **打开项目渐进加载**：进入项目不再等全部图片读取、解码完毕——先以占位条目
  立即进入编辑页，文件读取与解码（尺寸 / 缩略图 / EXIF）转后台按受限并发
  渐进补齐；首张（当前照片）优先处理，进入后即刻可看；图库缩略图逐张就位。
  安卓等慢存储上大项目的打开速度显著提升；切换 / 关闭项目后旧的后台任务作废
- **界面精简**：删除全部说明文案——导入浮层、裁剪 / 调节 / 水印面板、导出面板、
  设置页、工作区引导页、水印工作室、曲线编辑器等处的操作提示与功能解释小字
  （加载 / 授权状态与错误提示保留），并清理对应的孤儿样式；
  空状态文案只保留必要信息
- **字重独立成行**：文字水印的「字重」滑杆从「字体」旁的两列网格中拆出，
  与「字距」「行高」同样独立整行展示，「字体」下拉改为全宽
- **「模板」按钮改名「应用模板」**：并从添加按钮行移到下方独立一行，
  与「存为模板」上下呼应

### 修复

- **应用模板无反应**：模板图层对象携带 Vue 响应式代理，`structuredClone` 克隆时
  抛出 `DataCloneError` 且被静默吞掉——点「完全覆盖 / 追加水印层」后图层、
  预览、弹层均无任何变化；现在改用 JSON 深拷贝（图层为纯 JSON 数据），
  应用失败时同时给出错误提示而不是无声失败

## [1.1.0] - 2026-09-22

### 新增

- **边框水印**：全新图层类型——在图片四周向外扩展画布（导出尺寸随之变大），
  四边各自独立的宽度（固定像素）与颜色；可叠加多层相框（图层列表越靠前越贴近照片），
  与裁剪 / 拉直、文字 / 图片水印自由组合，预览与导出同一渲染管线；
  默认「四边同步」——调整任一边四边的宽度与颜色一起变化，
  断开后各边独立调整，四边出现不一致时自动断开、重新调齐后可再次开启
- **模板选择器**：操作面板的「模板」从下拉栏改为弹层选择器——列出全部模板
  （名称 / 图层数 / 内置标识），选中后询问「完全覆盖」还是「追加水印层」；
  覆盖与追加都作用于当前编辑目标（全局或这张照片的独立水印副本），
  「存为模板」同样按编辑内容保存
- **内置边框模板**：水印工作室新增「细白边框」与「画廊双框」（内衬 + 外沿两层叠加）
- **裁剪独立功能页**：操作面板新增「裁剪」标签（位于「调节」之后），进入即裁剪模式——
  直接在照片上拖动裁剪框，四角与四边手柄调整范围、框内拖动移动位置、
  框外区域压暗并叠加三分参考线，页内第一行实时显示裁剪后的像素尺寸
- **翻转与拉直**：裁剪页提供水平 / 垂直翻转按钮与 ±45° 拉直滑杆；
  拉直时裁剪框自动收缩到无透明角落的最大内接矩形，拖拽范围同样受限，
  不会在导出图中带出黑角
- **裁剪比例预设**：自由 / 1:1 / 4:3 / 3:4 / 16:9 / 9:16，选择后裁剪框立即调整为该比例的
  最大内接矩形；比例锁定下拖拽始终严格保持比例，到边界自动回缩
- **裁剪随照片独立保存**：与「单独调节」同一生命周期，预览、图库缩略图与导出所见即所得；
  水印字号与画布换算按裁剪后的照片尺寸进行，与最终输出一致；
  裁剪编辑中渲染「翻转 + 拉直后的全图」便于查看框外内容；
  水印在裁剪模式下即按裁剪后区域定位，应用前后位置一致
- **去雾 / 清晰度 / 锐化 / 颗粒**：调节面板新增四个滑块——去雾（拉黑场提对比），
  清晰度（大半径局部对比度）、锐化（小半径细节强化）、颗粒（亮度加权噪点），
  与既有调节共用单遍像素管线，预览与导出同一实现
- **水印单独调节**：水印面板新增「单独水印这张照片」开关——开启后当前照片使用独立的图层副本，
  对它的增删改不再影响其他照片，也不再应用全局水印，关闭即恢复；
  编辑上下文跟随选中的照片自动切换（选中带独立水印的照片时面板自动编辑其副本），
  导出时同样按照片生效
- **独立处理标识**：图库缩略图右上角为开启「单独调节」的照片显示滑杆小图标、
  开启「单独水印」的照片显示水滴小图标、已裁剪 / 翻转 / 拉直的照片显示裁剪小图标；
  左下角信息栏的分辨率同步显示裁剪后的输出尺寸

### 变更

- **缩放倍率药丸**：默认只显示数字（不预留箭头位置），鼠标移近时药丸扩宽、
  小箭头滑入，点击弹出缩放预设
- **移动端面板让位**：底部抽屉弹起时，照片预览中心自动上移到抽屉上方的剩余区域，
  收起后平滑归位；空状态问候页同样上移
- **加载提示**：打开工作项目时页面显示居中的加载动画；预览位图渲染期间
  （低清刷新与高清精修）左下角显示「渲染中」小徽章
- **工作室样张重绘**：默认样张改为「黄昏海面」（暮蓝到暖橙的天空、太阳光晕、
  远山剪影与海面碎光），水印效果展示更直观
- **操作面板顶栏纯图标化**：「水印 / 调节 / 裁剪 / 导出」四个标签只保留图标，
  功能名称移到各功能页第一行作为标题（水印 / 调节 / 裁剪 / 导出），
  「单独调节 / 单独水印」开关并入标题行右侧
- **水印偏移改为固定像素**：「距锚点 · 水平 / 垂直」由「图片长边百分比」改为固定像素值——
  设置 100px 即所有照片上水印都离锚点 100px，不再随图片尺寸或比例缩放；
  画布拖拽与面板数值均按像素换算，滑杆量程扩大到 ±1200px；
  旧配置与自建模板按标称长边 1000px 一次性折算迁移，内置模板数值同步更新

### 修复

- **文字 / 图片水印预览闪烁**：调节时的低清即时渲染会把已显示的高清位图替换成
  更小的档位，停顿后又换回高清——画面忽糊忽清地闪跳；现在即时渲染的分辨率
  不低于当前显示位图，替换时尺寸不变、只有内容更新，文字与图片水印不再闪烁
- **水印工作室不显示边框层**：工作室样张走主线程矢量绘制，边框层被跳过导致
  完全不可见；现在样张按「照片 + 边框」的最终画布合成（与 Worker 同一绘制顺序），
  视图适应、指针命中与拖拽换算同步到照片区域坐标，文字字号换算也按含边框的
  最终长边进行，与导出一致
- **移动端打开项目偶发失败**：读项目清单的异常没有捕获，失败时无任何提示、
  界面停在原处；现在捕获并弹出错误提示，可再次点击重试
- **边框水印的预览与导出比例不符**：预览渲染会把照片缩放到视口长边，但边框宽度
  按原始像素直接画上去——大尺寸照片在预览中的边框明显比导出结果粗；
  现在边框宽度按取样缩放系数等比折算，预览与导出的边框比例严格一致
  （断言：60px 边框在 4000px 照片的 1600px 预览中精确为 24px）
- **图库缩略图不跟随几何调整**：缩略图只在导入时生成一次，裁剪与边框调整后
  构图脱节；现在监听照片的几何签名（裁剪 / 翻转 / 拉直 / 边框），
  变化后 500ms 防抖按输出构图重新合成缩略图（新增几何合成器，
  与 Worker 管线同一套变换逻辑）
- **裁剪 / 拉直后预览与其他功能区不更新**：裁剪区域以响应式代理对象直接传入渲染 Worker，
  结构化克隆失败且异常被静默吞掉，导致该照片的后续渲染（含调节触发的重渲染）全部中断、
  画面停留在最后一次成功的结果；现在传 Worker 前统一拍平为纯数据，
  导出链路同理，渲染失败也会输出到控制台便于排查
- **拖动调节滑杆预览越来越迟滞**：拖动时每帧都向 Worker 提交一个低清渲染任务，
  处理速度跟不上提交速度，任务排队堆积、延迟越滚越大；现在同一时刻只保留一个
  在途低清任务，完成后若参数又变过立即用最新参数补一帧，
  高清精修的防抖延长到 300ms（拖动停止后约 0.3s 出精修画面）

### 其他

- 按「反 AI 模板感」清单扫描全库 UI 代码：确认无机器默认痕迹，
  既有克制的单强调色设计系统（层级靠字号字重间距表达）全部保留
- 版本号同步至 1.1.0（package.json / tauri.conf.json / Cargo.toml / build.gradle / version.ts，安卓 versionCode 8）

## [1.0.0] - 2026-09-21

首个正式版：功能与体验打磨到位后的里程碑，版本号进入 1.x。

### 新增

- **水印工作室模板库**：工作室改为两级视图——默认进入模板管理页（卡片网格，真实样张缩略图、图层数与日期），新建 / 编辑 / 复制 / 重命名 / 删除（带确认）一应俱全；内置模板受保护，保存自动转为「另存为副本」
- **数值拖拽微调**：全部滑杆的数值支持按住左右拖动细调（拖满 300px 走完量程，按步长取整），双击键入与双击复位不受影响
- **文字框锚点**：位置与变换区为文字层新增九宫格，决定文字框（含背景条）的哪个位置对准定位点，默认居中与既有行为一致，选角时文字向另一侧展开
- **导出自定义长边**：「长边限制」更名为「图像长边像素」，预设档旁可键入任意 0–20000 的自定义值（0 表示原图）
- **文件名模板令牌**：文件名模板输入框下方新增可点按的 EXIF 令牌，在光标处插入

### 修复

- **空状态问候页不随面板让位**：悬浮面板展开时只有照片会偏移让位，问候语的图标与文字仍停在原位被面板压住；现在与预览画布共用同一让位机制——面板展开、换边时问候内容（品牌图标 + 文案）一起平滑偏移到剩余空白一侧，收起后沿同一路径归位
- **「水印 / 调节 / 导出」标签过于贴边、可读性差**：面板顶栏的三个切换按钮此前没有水平内边距，文字紧贴圆角边缘；现在增加 14px 水平内边距，字号 12.5→13px、高度 28→30px、图标 14→15px，间距同步加大，小尺寸下也不再拥挤
- 模板库缩略图渲染不再依赖 requestAnimationFrame 时序，后台窗口 / 无焦点标签页下也能正常绘制

### 变更

- **标签列数自适应**：「水印 / 调节 / 导出」切换按钮的列数由硬编码 3 列改为随选项数量生成，组件在其它场景可复用
- **模板与水印统一**：原工作室「保存为水印」（预设）与「模板」合并为一套模板库，启动时自动迁移旧数据；主界面「模板」下拉覆盖全部模板
- **图层重命名**：双击图层列表项的名称即可行内重命名（自定义名优先显示）
- 「水印 / 调节 / 导出」标签列数随选项生成，字号与内边距加大；锚点选择器在面板内居中；文字内容输入框与面板等宽
- 「字重」标签去掉括号内的补充说明

### 其他

- 移除设置页分组标题上对中文无效的 `text-transform: uppercase`（英文界面模板残留）
- 版本号同步至 1.0.0（package.json / tauri.conf.json / Cargo.toml / build.gradle / version.ts，安卓 versionCode 7）

## [0.6.0] - 2026-09-20

### 新增

- **启动动画**：品牌标记淡入就位后整层快速淡出，露出主界面（总时长不足一秒，跟随系统减弱动态偏好）
- **顶层页面切换过渡**：主视图 ⇄ 水印工作室 ⇄ 设置之间增加非线性交叉过渡（淡入缩放、进出路径互为镜像，与既有工作区过渡同一套缓动体系）
- **水印工作室预览缩放**：样张支持滚轮缩放（锚定光标位置）、空白处拖拽平移、双指捏合与双击复位；放大后可平移查看样张边缘，平移到边界有橡皮筋回弹
- **安卓原生返回**：接入 `@capacitor/app`，返回键 / 手势按「右键菜单 → 导入弹层 → 逐级回页 → 收起面板」逐级回退，最后一步保存数据后退出
- **DMG 内置修复脚本**：macOS 构建的 DMG 里附带「修复已损坏提示.command」，双击即可移除隔离标记（`xattr -cr`），解决未签名应用提示「已损坏，无法打开」的问题

### 修复

- **切到其他页面（设置 / 水印工作室）后悬浮面板不可拖动**：工作区容器随页面切换卸载重建后，尺寸观察仍挂在旧元素上、夹紧宽度过期，拖动被锁死；现在每次重挂载都重新观察并实测宽度，拖动夹紧也改用按下时的实测值
- **收起把手不跟吸附边**：上述宽度过期还会把面板位置重置到左侧而吸附边仍记在右侧，导致「面板在左、把手在右」；一并修复
- **安卓竖屏收起把手点不到**：面板收起后透明的位置壳层仍覆盖在把手上方拦截点击；壳层不再拦截指针，并把把手与底部抽屉移入手势条安全区内
- **设置页返回按钮按不了（安卓全面屏）**：设置页头部未避开状态栏（对比水印工作室缺少顶部安全区留白），返回按钮被系统状态栏覆盖；已补齐安全区留白
- **主预览放大后部分内容无法平移查看**：悬浮面板展开时，平移松手回弹以全视口为基准（与拖动中的让位基准不一致），图片可以被放行到面板底下；基准统一到让位后的可用区域，面板开合动画结束后也会收紧一次位置
- **安卓端没有持久化**：启动恢复逻辑被锁定在桌面端（`isTauri`）之后，安卓每次启动都不读取已写入的数据目录；现在安卓同样在启动时恢复工作目录与项目

### 变更

- **安卓退出前保存**：项目清单的防抖保存增加 flush 机制，返回键退出、切后台（`pagehide` / `visibilitychange`）时立即写出，防止 400ms 窗口内被系统杀掉丢数据

### 其他

- 版本号同步至 0.6.0（package.json / tauri.conf.json / Cargo.toml / build.gradle / version.ts，安卓 versionCode 6）
- 新增运行时依赖 `@capacitor/app`（安卓返回键适配）

## [0.5.0] - 2026-09-20

### 新增

- **安卓端完整工作区**：接入 `@capacitor/filesystem`，工作目录与工作项目在应用数据目录内持久化（复制模式），不再只是会话级临时项目
- **多工作目录**：可同时记录多个工作目录，以列表统一管理（路径 / 项目数 / 一键切换 / 移除记录不删文件）；添加目录不再要求空文件夹，已包含辑印项目的文件夹会被自动识别
- **删除移入回收站**：删除工作项目改为移入回收站（桌面为系统回收站，安卓为应用内回收站文件夹），项目内照片移除同理，不再直接抹除
- **悬浮面板拖动吸附**：按住面板顶栏即可拖动，松手按释放动量吸附到左缘或右缘（弹簧动画衔接，可随时反向抓回）
- **预览中心让位**：面板展开时照片中心自动偏移到剩余空白一侧并重新适应可视大小，收起或换边时平滑归位
- **软件自定义右键菜单**：全局禁用原生右键菜单；预览区（适应窗口 / 100% / 导入 / 收展面板）与缩略图（设为当前 / 移除）各有上下文菜单，文本编辑场景保留系统菜单
- **缩放预设菜单**：左下角缩放值可点击，悬停浮现小箭头，弹出 50% / 100% / 125% / 150% / 适应窗口
- **设置独立页面**：设置从弹窗改为全页，分区呈现外观 / 预览 / 水印与导入 / 数据 / 关于
- **关于与检查更新**：关于分区含软件介绍、版本号与 GitHub Releases 更新检查，发现新版本可直达下载页
- **自定义显示字体**：设置中可为界面选择字体，自动读取系统字体并按书写体系分类
- **行内重置按钮**：调节面板每个选项的数值旁在偏离归位值时显示重置按钮，归零后隐藏
- **顶栏水印工作室入口**：「水印工作室」按钮加入顶栏药丸工具栏（位于导入图片之后）

### 变更

- **界面底色**：浅色主题改为纯白、深色主题改为纯黑，照片成为唯一视觉焦点；照片绘制增加淡投影与细描边，纯白底上边界依然清晰
- **悬浮面板质感**：面板填充改为完全不透明实底，阴影明显调淡
- **空状态文案**：问候语后不再拼接项目名，提示改为「选择或拖入照片，开始今天的辑录。」
- **入库方式后置**：空状态页不再常驻「照片入库方式」选择，选片 / 拖入后先弹出导入预览窗，确认数量与入库方式后才写入项目
- **水印面板精简**：删除面板内的说明文案与「工作室」按钮（入口移至顶栏），直接铺开图层编辑器
- **OOBE 品牌动画**：仅保留两侧三角形的甩出动画，中间四边形色块保持静止
- **标签间距**：悬浮面板「水印 / 调节 / 导出」三个标签的间距加大，不再拥挤

### 修复

- 曲线编辑器控制点被拉伸为椭圆、拖动不跟手的问题（位图固定尺寸被拉伸、指针映射未扣除内边距所致）
- 通过链接导入照片后缩略图不自动刷新的问题（缩略图补写绕过了响应式代理）
- 悬浮面板弹出意外横向滚动条的问题
- 导出面板全部元素失去与面板边距的问题（缺少统一内边距）

### 其他

- 文件 IO 收敛到 `core/fs.ts` 跨平台抽象（Tauri plugin-fs / Capacitor Filesystem），Rust 侧新增 `move_to_trash` 命令（`trash` crate）
- 移除 Logo 悬停的过冲缓动曲线，悬停反馈更克制
- 版本号同步至 0.5.0（package.json / tauri.conf.json / build.gradle / Cargo.toml）

## [0.4.0] - 2026-09-19

### 新增

- **工作目录 / 工作项目两级结构**：先选一个**空文件夹**作为工作目录（软件自动校验，
  记录进软件数据，下次启动直达），目录里可创建任意多个工作项目（子文件夹），
  项目管理页以卡片排列（名称 / 照片数 / 创建时间），支持新建、进入、删除与随时更换目录；
  项目内导入照片时逐批选择**复制原文件**或**链接源文件路径**，两者可在同一项目内共存，
  缩略图右下角以小图标区分入库方式
- **对比度与色调曲线**：调节面板新增对比度滑杆与可拖拽的曲线编辑器
  （空白处加点、拖动调整、双击去点），曲线用单调三次样条采样成查找表，
  预览与导出共用同一实现；曲线随「单独调节」一同按照片独立保存
- **系统字体自动读取**：文字水印字体改为自动扫描系统已安装字体
  （Local Font Access），按**中文 / 西文 / 日文 / 韩文 / 等宽**分组展示；
  被拒或不支持的环境回退到常用字体清单，仍可手动键入字体名
- **字重无极调节**：字重由五档下拉改为 100–900 连续滑杆，可变字体完整生效，
  静态字体自动落到最近字重
- **文字大小改用 px**：以当前参照图长边的像素值呈现，所见即所得
- **数值直接键入**：所有滑杆数值**双击**即可用键盘输入精确值（自动限位与按步长取整）
- **方向键切换照片**：← / → 顺序切换当前照片（循环）
- **实时缩放值**：预览左下角常驻显示当前缩放百分比
- **悬浮操作面板**：水印 / 调节 / 导出面板改为悬浮圆角玻璃层，可一键收起为右缘把手，
  进入工作区管理页面时自动收起；移动端变为底部浮层
- **流畅过渡动画**：OOBE → 选择工作目录 → 项目管理三步之间按前进 / 返回方向做
  非线性推移动画；工作区 ⇄ 项目、面板内三个功能页切换均有淡入缩放过渡
  （跟随系统减弱动态偏好）
- **OOBE 品牌动画**：首屏 Logo 的两个小三角形与色块交替甩出回位，
  Expo-out 缓动的循环动画带出速度感（仅 OOBE 首屏开启）
- 预览画布尊重系统安全区：适配与命中计算避开系统手势条

### 变更

- **水印偏移按长边换算**：距锚点偏移改按图片长边百分比存储，
  横竖屏切换时两个方向的像素数值保持一致、不再互换
- **鼠标中键专用于平移**预览画面，不再触发图层选中 / 拖动
- 工作室样张画布尺寸改为显式锁定，随容器自适应且不再抖动

### 修复

- **OOBE 读不到便携版 exe 根目录**：官方 `path.executableDir` 底层在
  Windows / macOS 返回空值，导致首屏数据位置显示「…」、最近数据静默不保存；
  改由 Rust 侧 `current_exe` 提供目录，并以系统数据目录兜底
- **标题栏无法拖动**：拖拽区属性只标在左侧一小段，整条标题栏现均可拖动
  （按钮与药丸组正常点击）
- **水印工作室水印无法拖动**：样张画布补上指针手势（命中测试 / 拖拽 / 吸附 / 选中框）
- **水印渲染不及时 / 切换预设消失**：图层变化改为同步重绘 + 素材异步补帧，
  应用预设、新建图层、调参均即时上屏

### 其他

- Android `versionCode` 升至 4，版本号同步 0.4.0

## [0.3.0] - 2026-09-19

### 新增

- **工作区**：启动软件先进入工作区（本质是一个存放数据的文件夹），之后才能导入照片、
  打开水印 / 调节 / 导出操作面板。可选择**复制照片进工作区**，或**按路径链接源文件**；
  链接的文件会生成内容**识别码**（SHA-256），源文件失联时可重新定位并用识别码校对
- **OOBE 首次引导**：桌面端首次启动选择软件数据位置，默认放在**便携版 exe 根目录**
  （`AlbumarkData` 子文件夹，随 U 盘携带），改选其他位置后软件自行记忆；
  保存工作区记录与水印预设
- **水印工作室**：完全独立的页面，脱离照片制作水印（样张预览 + 图层编辑），
  制作结果存入软件数据（预设），工作区面板中随时调用
- **操作面板三合一**：水印 / 调节 / 导出三个切换按钮收进侧栏，主题色强调选中项；
  导出从独立弹窗并入面板
- **自定义标题栏**：桌面端取消原生标题栏（无边框窗口），自绘窗口控制按钮与拖拽区；
  「工作区 / 导入图片 / 设置」三个控件收进**药丸形集合组**
- **水印锚点定位**：九宫格锚点 + 「距锚点水平 / 垂直像素距离」确定水印位置，
  预览画布实时显示锚点十字；不同尺寸照片按比例换算
- **水印吸附**：拖动时自动吸附图片中线、安全边距与其他图层中心，显示虚线参考线；可在设置中开关
- **EXIF 缺失常驻警告**：文本内容用到的令牌在当前照片上解析不出时，
  水印内容区顶部常驻提示缺哪些项
- **预览安全区**：设置中可自定义图片最小缩放值（30%–100%）
- **预览性能档位**：高质量 / 均衡 / 省电三档（预览分辨率与 DPR 上限不同），
  Android 等低端设备可自定义调低，降低拖动发热与掉帧
- **文字水印自定义字体**：支持扫描系统已安装字体（WebView2 等内核）或手动输入字体名称
- **图库信息栏**：左下角显示当前照片的分辨率 · 文件名 · EXIF 摘要（机型 · 焦距 光圈 快门 ISO）

### 变更

- **分层渲染**：预览拆为底图 + 水印两层——Worker 只负责底图（解码 + 调节），
  水印叠加层在主线程矢量绘制；拖动水印只重绘叠加层，不再触发 Worker 全图重渲染，彻底告别卡顿
- **文字水印碰撞箱贴合文字**：包围盒改用真实字形上升 / 下降高度度量，
  行高只作用于行间距，不再把首尾行距包进碰撞箱
- **「底色条」更名为「背景填充」**
- 滑动条全部重绘：凹槽轨道 + 实心填充 + 带描边拇指，按压放大 + 主题色光环反馈
- Logo 增加悬停 / 点击非线性反馈动画（回弹曲线，按下即时缩放）
- Android 版本号同步 0.3.0

### 修复

- **Android 桌面图标缺失**：`drawable-v24/ic_launcher_foreground.xml` 是 Gradle 模板默认图标，
  minSdk 24 下它覆盖了品牌图标——删除后自适应图标恢复品牌前景

## [0.2.0] - 2026-09-19

### 新增

- **预览缩放与平移**：PC 端滚轮 / 触控板捏合缩放，移动端双指捏合，双击（双触）在适应窗口与
  250% 间切换；放大后可拖拽平移，边界有橡皮筋回弹，放大时自动请求高分辨率精修
- **单独调节**：调节面板可对当前照片开启独立参数（从全局值复制起步），开启后不再应用全局调节，
  预览与导出均按每张照片的设置生效
- **软件设置**：工具栏新增设置入口，可切换外观主题（跟随系统 / 深色 / 浅色）、
  开关「EXIF 缺失提醒」，并展示版本信息
- 导入照片缺少 EXIF 拍摄信息时给出提示，可关闭
- 批量导入自动生成缩略图（240px 长边），图库改用缩略图渲染

### 变更

- 应用图标重绘：白色背景 + 居中品牌图形；桌面 icns / ico / png 与 Android 启动图标同步更新，
  Android 自适应图标改用矢量前景（`drawable/ic_launcher_foreground.xml`）
- 导出图标改为向外箭头（上传样式）
- 主按钮去除投影；全部按钮的悬停反馈改用更流畅的非线性缓动（`--ease-soft`，180ms）
- Android 适配状态栏与手势区：内容延伸至系统栏之下，安全区边距经 Capacitor 8 SystemBars
  以 CSS 变量透传，状态栏图标样式随应用主题切换
- 启动欢迎语按系统时间问好（早晨 / 下午 / 晚上等），替代固定文案
- Android `versionCode` 升至 2

### 修复

- Windows 便携版启动不再弹出命令行终端窗口（`windows_subsystem` 归位为 crate 级内层属性）
- 批量导入大量照片时不再卡顿：导入即上屏占位，尺寸 / 缩略图 / EXIF 由并发池处理，
  不再在主线程逐张串行解码原图

### 其他

- GitHub Actions 多平台构建（Windows x64 便携版 / macOS arm64 / Android APK），
  应用标识统一为 `com.mws.albumark`

## [0.1.0] - 2026-09-18

首个公开版本。

### 新增

- **批量导入**：拖拽（含文件夹递归）/ 选择器 / 图片链接三种方式，仅支持 JPG；
  自动提取 EXIF 摘要（机型、镜头、光圈、快门、感光度、焦距、拍摄日期、作者）
- **水印工作室**：
  - 文本图层（字体 / 字重 / 颜色 / 字距 / 行高 / 对齐 / 斜体 / 描边 / 阴影 / 底色条）
  - 图像图层，支持自定义 SVG（矢量栅格化）与 PNG / JPG 贴纸，可替换
  - EXIF 令牌系统，导出时按每张照片逐一解析替换
  - 画布直接拖拽 + 九宫格定位 + 缩放 / 旋转 / 不透明度 / 混合模式
  - 平铺整图模式（间距可调）
  - 模板系统：内置「右下签名 / 全图平铺 / EXIF 参数条」，支持自制模板持久化
- **快速辑录**：亮度、曝光、阴影、高光、色温、色调、晕影七项调节，
  低分辨率即时预览 + 高清精修
- **打包交付**：JPEG 质量与长边限制、文件名模板与自动去重、
  ZIP 压缩包（全平台）/ 文件夹直写（桌面端）、并行渲染与进度展示
- 技术底座：Vue 3 + TypeScript + Vite；Web Worker 单遍渲染管线；
  Tauri 2 桌面脚手架（官方插件，零自定义 Rust 命令）；Capacitor 8 安卓壳工程；
  深浅色自适应 UI，唯一品牌橙 `#FFA72F`

### 已知限制

- 导出不回写 EXIF；不支持 RAW / HEIC 输入；水印与调节为全局配置
